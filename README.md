# ics-render

Parse one or more ICS calendar files, merge their events, and print a table sorted by start time.

## Install

```bash
pip install -e ".[dev]"
```

## Usage

```bash
ir_format --filepath calendar-a.ics --filepath calendar-b.ics
```
